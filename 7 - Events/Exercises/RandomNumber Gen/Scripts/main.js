function $(id) {
    return document.getElementById(id)
}

//Assign events to html elements
window.onload = function () {
    //button event (to generate a random number)

    //slider events (you need two, one for each slider)
}


function updateLimits() {
    //get the value of each slider 

    //update the text that displays their current value
}


function generateRandom() {
    let min = //get value of the first slider
    let max = //get value of the second slider

    // this line will generate a random number between min and max values.
    let random_number = Math.floor(Math.random() * (max - min + 1)) + min


    // handle cases where min is greater than max by showing an error message to the user
}

/*
    Area for the second example (random number generator)
*/




